---
// /lab — AI code tinkering index. Same ledger grammar as /work, kept as a
// fully separate route + list so personal projects never interleave with
// professional case studies. See docs/plans/2026-07-03-console-mvp.md.

import StatusChip from "../../components/StatusChip/StatusChip.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { listProjects, projectHref, projectStatus } from "../../lib/projects";

const projects = (await listProjects()).filter((p) => p.category === "side");

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "@id": "https://korabeland.com/lab",
  url: "https://korabeland.com/lab",
  name: "lab · AI code tinkering · korabeland.com",
  description:
    "Personal AI projects, built to learn how these systems behave, not to bill hours.",
  isPartOf: { "@id": "https://korabeland.com/#website" },
};
---

<BaseLayout
  title="lab · AI code tinkering · korab eland"
  description="Personal AI projects, built to learn how these systems behave, not to bill hours."
  active="lab"
>
  <script type="application/ld+json" slot="jsonld" set:html={JSON.stringify(jsonLd)} />

  <div class="work">
    <section class="work-head">
      <p class="t-label work-eyebrow">lab · AI code tinkering</p>
      <p class="work-intro">
        Personal projects, not client work. Built on my own time to learn how these systems
        behave, broken and rebuilt along the way.
      </p>
    </section>

    <section class="ledger-section" aria-labelledby="ledger-heading">
      <div class="section-head">
        <h2 id="ledger-heading" class="t-label">all tinkering</h2>
      </div>

      {
        projects.length > 0 ? (
          <ol class="ledger">
            {projects.map((p) => {
              const status = projectStatus(p);
              return (
                <li class="ledger-row">
                  <a class="ledger-link" href={projectHref(p)}>
                    <span class="ledger-name">{p.title}</span>
                    <span class="t-mono ledger-datum">{p.outcome}</span>
                    <StatusChip status={status} variant="chip" />
                  </a>
                </li>
              );
            })}
          </ol>
        ) : (
          <p class="t-mono ledger-empty">first tinkering project being written up.</p>
        )
      }
    </section>
  </div>
</BaseLayout>

<style>
  .work {
    max-width: 960px;
    margin-inline: auto;
    padding: var(--s-7) var(--s-4) var(--s-8);
    display: flex;
    flex-direction: column;
    gap: var(--s-7);
  }

  .work-head {
    display: flex;
    flex-direction: column;
    gap: var(--s-4);
  }

  .work-eyebrow {
    margin: 0;
  }

  .work-intro {
    font-size: 17px;
    line-height: 1.6;
    color: var(--ink-soft);
    max-width: 60ch;
    margin: 0;
  }

  .section-head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: var(--s-4);
    border-bottom: 1px solid var(--ink);
    padding-bottom: var(--s-2);
  }

  .section-head h2 {
    margin: 0;
    color: var(--ink-soft);
  }

  .ledger {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .ledger-row {
    border-bottom: 1px solid var(--rule);
  }

  .ledger-link {
    display: grid;
    grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr) auto;
    align-items: baseline;
    gap: var(--s-5);
    padding: var(--s-4) var(--s-2);
    text-decoration: none;
    transition: background 150ms ease;
  }

  @media (hover: hover) {
    .ledger-link:hover {
      background: var(--paper-2);
    }
  }

  .ledger-name {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 17px;
    color: var(--ink);
    line-height: 1.35;
  }

  .ledger-datum {
    text-transform: none;
    letter-spacing: 0.02em;
    color: var(--ink-soft);
  }

  .ledger-empty {
    padding: var(--s-5) var(--s-2);
    margin: 0;
  }

  @media (max-width: 680px) {
    .ledger-link {
      grid-template-columns: 1fr auto;
    }
    .ledger-datum {
      grid-column: 1 / -1;
      order: 3;
    }
  }
</style>
